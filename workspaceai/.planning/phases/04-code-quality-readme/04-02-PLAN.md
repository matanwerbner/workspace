---
phase: 04-code-quality-readme
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shell/Sidebar.tsx
  - src/state/store.ts
  - src/views/browser/BrowserView.tsx
autonomous: true
requirements:
  - QUALITY-04

must_haves:
  truths:
    - "No `console.error`, `console.warn`, or `console.log` calls exist in non-test source files under src/ or electron/"
    - "All error/warn/info events previously emitted to the console are now routed through `log()` from `ipc/client`"
    - "The redundant `console.error` in store.ts (line 366) is removed because the `log()` call directly above it already records the error"
  artifacts:
    - path: "src/shell/Sidebar.tsx"
      provides: "Uses log() for workspace creation errors"
      contains: "log('workspace', 'create:error'"
    - path: "src/state/store.ts"
      provides: "Uses log() for import warnings; redundant console.error removed"
      contains: "log('workspace', 'import:missing-paths'"
    - path: "src/views/browser/BrowserView.tsx"
      provides: "Uses log() for webview console messages"
      contains: "log('browser', 'webview:console'"
  key_links:
    - from: "src/shell/Sidebar.tsx"
      to: "src/ipc/client"
      via: "log() import"
      pattern: "import.*log.*ipc/client"
    - from: "src/views/browser/BrowserView.tsx"
      to: "src/ipc/client"
      via: "log() import"
      pattern: "import.*log.*ipc/client"
---

<objective>
Replace every `console.error`, `console.warn`, and `console.log` call in non-test source code with the project's `log()` function from `src/ipc/client`. The session log file is the single observability channel; raw console output is not captured in production.

Purpose: Ensures all runtime events are observable through the session log file (`~/Library/Application Support/workspaceai/logs/`), consistent with the logging convention already followed everywhere else in the codebase.

Output: Zero `console.*` calls in `src/` and `electron/` outside of test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-code-quality-readme/04-RESEARCH.md
@.planning/phases/04-code-quality-readme/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace console.* calls with log() in Sidebar.tsx and store.ts</name>

  <read_first>
    - src/shell/Sidebar.tsx — full file (confirm log is not imported yet; find the console.error on ~line 100)
    - src/state/store.ts — lines 355–390 (importWorkspace function with console.error line 366 and console.warn line 382)
    - src/ipc/client.ts — confirm the `log` export signature: `log(category, action, detail?, level?)`
  </read_first>

  <files>src/shell/Sidebar.tsx, src/state/store.ts</files>

  <action>
    Sidebar.tsx changes:
    1. `log` is not yet imported in Sidebar.tsx. Add `import { log } from '../ipc/client';` to the existing import block (alongside the existing `import { api } from '../ipc/client';` — these can be merged into one import from the same module or kept as a second import line; either is fine).
    2. Find the `.catch` handler around line 99–101 that calls `console.error('Failed to create workspace:', err)`. Replace the `console.error` line with: `log('workspace', 'create:error', { error: err instanceof Error ? err.message : String(err) }, 'error');`

    store.ts changes:
    1. `log` is already imported in store.ts (it is used throughout the file). Do not add another import.
    2. On line 366, there is a `console.error('Workspace import failed:', e)` immediately after a `log('workspace', 'import:error', ...)` call on line 365. The console.error is redundant — delete it entirely (line 365's log call already records the failure).
    3. On lines 381–383, replace the `console.warn('Imported workspace has missing paths:', result.missingPaths)` statement with: `log('workspace', 'import:missing-paths', { paths: result.missingPaths }, 'warn');`
  </action>

  <verify>
    <automated>cd /Users/matanw/projects/workspace/workspaceai && grep -n "console\." src/shell/Sidebar.tsx src/state/store.ts | grep -v "\.test\." | wc -l</automated>
    <automated>cd /Users/matanw/projects/workspace/workspaceai && npm test -- src/shell/Sidebar src/state/__tests__ 2>&1 | tail -8</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "console\." src/shell/Sidebar.tsx` returns 0 results
    - `grep -n "console\." src/state/store.ts` returns 0 results
    - `grep "log('workspace', 'create:error'" src/shell/Sidebar.tsx` returns 1 result
    - `grep "log('workspace', 'import:missing-paths'" src/state/store.ts` returns 1 result
    - `grep "console.error.*Workspace import" src/state/store.ts` returns 0 results
    - All tests in `src/state/__tests__/` continue to pass
  </acceptance_criteria>

  <done>Sidebar.tsx and store.ts contain zero console.* calls; all log routing uses log() from ipc/client.</done>
</task>

<task type="auto">
  <name>Task 2: Replace console.log in BrowserView.tsx with log()</name>

  <read_first>
    - src/views/browser/BrowserView.tsx — lines 110–135 (the onConsole handler with console.log and eslint-disable comment)
    - src/ipc/client.ts — confirm `log` export and the relative path from BrowserView.tsx (`../../ipc/client`)
  </read_first>

  <files>src/views/browser/BrowserView.tsx</files>

  <action>
    1. In `src/views/browser/BrowserView.tsx`, add `log` to the imports from `../../ipc/client`. Check the existing imports — if `../../ipc/client` is not yet imported, add `import { log } from '../../ipc/client';`.
    2. Find the `onConsole` handler around lines 113–119. It currently contains:
       ```
       // eslint-disable-next-line no-console
       console.log('[webview]', detail.message);
       ```
       Replace both the `// eslint-disable-next-line no-console` comment and the `console.log` line with:
       `log('browser', 'webview:console', { message: detail.message });`
    3. Do not change the surrounding event-listener registration or the function signature.
  </action>

  <verify>
    <automated>cd /Users/matanw/projects/workspace/workspaceai && grep -n "console\." src/views/browser/BrowserView.tsx | wc -l</automated>
    <automated>cd /Users/matanw/projects/workspace/workspaceai && grep -n "eslint-disable.*no-console" src/views/browser/BrowserView.tsx | wc -l</automated>
    <automated>cd /Users/matanw/projects/workspace/workspaceai && npm run typecheck:web 2>&1 | grep "BrowserView" | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "console\." src/views/browser/BrowserView.tsx` returns 0 results
    - `grep -n "eslint-disable.*no-console" src/views/browser/BrowserView.tsx` returns 0 results
    - `grep "log('browser', 'webview:console'" src/views/browser/BrowserView.tsx` returns 1 result
    - `npm run typecheck:web` reports no errors in BrowserView.tsx
  </acceptance_criteria>

  <done>BrowserView.tsx routes webview console messages through log() into the session log file; no raw console.* calls remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webview → log() | Webview console messages are now forwarded into the session log. The message content is untrusted (from page scripts). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Information Disclosure | BrowserView.tsx webview:console | accept | Log detail is written to local session log only; not transmitted externally. Message is truncated to the string the browser provided — no eval or execution. |
| T-04-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan. |
</threat_model>

<verification>
```bash
# Zero console.* calls outside test files
cd /Users/matanw/projects/workspace/workspaceai
grep -rn "console\." src/ electron/ | grep -v "__tests__\|\.test\." | grep -v "node_modules"

# Should return 0 lines
```
</verification>

<success_criteria>
- `grep -rn "console\." src/ electron/ | grep -v "__tests__\|\.test\."` returns zero results
- `npm test` all passing (no regressions from the deletions)
- `npm run typecheck` still clean after these changes
</success_criteria>

<output>
Create `.planning/phases/04-code-quality-readme/04-02-SUMMARY.md` when done.
</output>
