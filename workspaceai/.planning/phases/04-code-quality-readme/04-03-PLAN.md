---
phase: 04-code-quality-readme
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - README.md
  - CLAUDE.md
autonomous: true
requirements:
  - QUALITY-05
  - QUALITY-06

must_haves:
  truths:
    - "README.md title is `# Orbit` (not `# WorkspaceAI`)"
    - "README.md lists all 5 current view types: code, browser, terminal, pdf, notepad"
    - "README.md describes the live Claude AI backend (streaming, agentic loop, tool calls) — not the old stub"
    - "README.md documents the IPC channel surface (with a channels table sourced from electron/preload.ts)"
    - "README.md describes workspace-level memory (homeFolder, MEMORY.md, read_memory/write_memory tools)"
    - "README.md describes session logging with correct field names: t, level, cat, action, detail"
    - "README.md includes Build & Package section covering `npm run dev`, `npm test`, `npm run typecheck`, `npm run dist:mac`"
    - "CLAUDE.md log entry shape shows `{ t, level, cat, action, detail }` (not `{ ts, level, category, action, detail }`)"
    - "CLAUDE.md grep examples use `\"cat\":\"chat\"` and `\"cat\":\"ipc\"` (not `\"category\":`)"
  artifacts:
    - path: "README.md"
      provides: "Accurate project documentation for Orbit"
      contains: "# Orbit"
    - path: "CLAUDE.md"
      provides: "Accurate log-field documentation"
      contains: "{ t, level, cat, action, detail }"
  key_links:
    - from: "README.md"
      to: "electron/preload.ts"
      via: "IPC channels table — sourced from the real preload surface"
      pattern: "ai:chat|terminal:create|memory:readIndex"
    - from: "CLAUDE.md"
      to: "electron/logger.ts"
      via: "Log entry shape documentation matches actual JSON emitted"
      pattern: '"cat":"chat"'
---

<objective>
Rewrite README.md to accurately describe Orbit as it stands today (5 view types, live Claude AI backend, workspace memory, session logging, terminal reconnect), and fix the CLAUDE.md log-field inaccuracy that causes grep commands to produce zero results on real session log files.

Purpose: The documentation is the developer-facing interface. Both files currently contain false information that wastes debugging time. These changes must happen last (wave 2) so they reflect the fully cleaned-up codebase from wave 1 plans.

Output: README.md is a complete, accurate guide to Orbit. CLAUDE.md grep examples work against real session log files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-code-quality-readme/04-RESEARCH.md
@.planning/phases/04-code-quality-readme/04-PATTERNS.md
@.planning/phases/04-code-quality-readme/04-01-SUMMARY.md
@.planning/phases/04-code-quality-readme/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CLAUDE.md log field name inaccuracy</name>

  <read_first>
    - CLAUDE.md — full file (short; the inaccurate shape and grep examples are in the Debugging Sessions section)
    - electron/logger.ts — lines 155–170 (the actual JSON shape emitted: "t", "level", "cat", "action", "detail")
  </read_first>

  <files>CLAUDE.md</files>

  <action>
    In the "Debugging Sessions" section of CLAUDE.md, make two targeted changes:

    1. Change the log entry shape documentation from:
       `Log entries follow the shape: { ts, level, category, action, detail }.`
       to:
       `Log entries follow the shape: { t, level, cat, action, detail }.`

    2. In the "To filter by category or level" bash block, update the two grep examples:
       - Change `grep '"category":"chat"'` to `grep '"cat":"chat"'`
       - Change `grep '"category":"ipc"'` to `grep '"cat":"ipc"'`

    Do not change any other content in CLAUDE.md (development workflow, testing section, etc.). The file title can remain "WorkspaceAI — Development Guide" or be updated to "Orbit — Development Guide" — use "Orbit" to match the rebranded app name.
  </action>

  <verify>
    <automated>cd /Users/matanw/projects/workspace/workspaceai && grep -c '"category"' CLAUDE.md || true</automated>
    <automated>cd /Users/matanw/projects/workspace/workspaceai && grep -c '"cat"' CLAUDE.md</automated>
    <automated>cd /Users/matanw/projects/workspace/workspaceai && grep "follow the shape" CLAUDE.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep '"category"' CLAUDE.md` returns 0 results
    - `grep '"cat":"chat"' CLAUDE.md` returns 1 result
    - `grep '"cat":"ipc"' CLAUDE.md` returns 1 result
    - `grep "follow the shape" CLAUDE.md` output contains `t, level, cat, action, detail`
  </acceptance_criteria>

  <done>CLAUDE.md grep examples produce real results against session log files. Log field names match electron/logger.ts output.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README.md to accurately describe Orbit</name>

  <read_first>
    - README.md — full current file (to understand what to replace)
    - electron/preload.ts — full file (IPC channel surface for the channels table)
    - src/views/*/index.tsx — typeId and label for all 5 view types (code, browser, terminal, pdf, notepad)
    - src/state/types.ts — PersistedAppState schema fields (v2, workspaces, homeFolder, memory)
    - package.json — scripts section (dev, build, dist, dist:mac, typecheck, test) and dependencies list
    - electron/logger.ts — lines 155–175 (session log shape, confirming t/cat field names)
    - .planning/phases/04-code-quality-readme/04-RESEARCH.md — README gap analysis table (Section "README.md — Gap Analysis")
  </read_first>

  <files>README.md</files>

  <action>
    Write a new README.md that replaces the entire current content. Use the structure below. All content must be sourced from the actual codebase (no invented details). Do NOT use fenced code blocks inside `<action>` — the target content is described by section below.

    SECTION: Title and tagline
    - Title: `# Orbit`
    - Tagline: A macOS container app where every view has its own dedicated Claude AI console. Supports code editing, browser, terminal, PDF reading, and notepad views — each with agentic tool calls, streaming responses, and per-workspace memory.

    SECTION: Stack
    List all relevant dependencies from package.json:
    - Electron + electron-vite
    - React 18 + TypeScript
    - Anthropic SDK (@anthropic-ai/sdk) — Claude streaming API
    - Monaco Editor (VS Code editor) for code views
    - xterm.js + node-pty for terminal views
    - pdfjs-dist for PDF views
    - marked for markdown rendering
    - Zustand + electron-store for state persistence
    - react-resizable-panels for the resizable chat split

    SECTION: View Types
    Table with columns: Type ID | Label | Description
    Source from the `registerView()` calls in `src/views/*/index.tsx`:
    - code | Code View | Monaco editor with file tree; run_command and fs tools
    - browser | Browser | Chromium webview with URL bar
    - terminal | Terminal | Full pty terminal (xterm.js + node-pty); reconnects across dev reloads
    - pdf | PDF | PDF viewer via pdfjs-dist with doc:// protocol
    - notepad | Notepad | Rich-text editor (Draft.js)

    SECTION: Architecture
    Include a file tree that shows the current directory structure (sourced from actual directory listing). Key directories: src/App.tsx, src/shell/ (Sidebar, MainPane, ChatPanel, AddViewModal, SettingsModal, memory.ts, termBuffer.ts), src/views/ (registry.ts, types.ts, code/, browser/, terminal/, pdf/, notepad/), src/state/ (store, types, migrate), src/ipc/client.ts, src/lib/ (markdown, models, uid, url, lang), src/components/, electron/ (main.ts, preload.ts, ipc/ with fs, ai, store, terminal, roots, workspace, memory, codeServer, doc, logger).

    Sub-section: IPC Model
    Describe the contextBridge pattern: main process handlers in `electron/ipc/*.ts`, bridged via `electron/preload.ts`, typed wrappers in `src/ipc/client.ts`. Include an IPC channels reference table with columns: Namespace | Channels | Purpose. Rows sourced from preload.ts:
    - dialog | pickFolder, pickFile, confirm | Native system dialogs
    - fs | listDir, readFile, writeFile, createFile, search, basename | File system (roots-gated)
    - store | get, set | App state persistence
    - ai | hasKey, setKey, clearKey, chat, cancelChat, chunk (push event) | Claude API streaming
    - terminal | create, reconnect, write, resize, kill, exec, data/exit (push events) | pty lifecycle
    - codeServer | start, stop, status | Embedded VS Code (code-server)
    - workspace | initHomeFolder, setActiveHomeFolder, export, import | Workspace management
    - memory | readIndex, readEntry, writeEntry | Per-workspace memory files
    - doc | allow, revoke, read | PDF doc:// protocol allow-list
    - shell | openExternal | Open URLs in system browser
    - log | event | Renderer → session log file

    Sub-section: Security Model
    - File-system access is gated by `electron/ipc/roots.ts` — only paths under registered roots are readable/writable
    - Renderer runs with `nodeIntegration: false`, `contextIsolation: true`
    - CSP blocks inline scripts; webview content cannot reach main-process APIs
    - Store file names use legacy `workspaceai-*` prefix (intentional — renaming would invalidate existing user installs)

    SECTION: AI Chat
    Describe the actual behavior (sourced from ChatPanel.tsx agentic loop):
    - Per-view chat history persisted across restarts
    - Streaming responses via `ai:chat` with `streamId` event pattern
    - Agentic loop: Claude may call tools (view-provided tools, run_command, memory tools); tool results fed back; loop runs until stop_reason is not `tool_use`
    - Tool calls require user approval (approve/deny prompt in ChatPanel) unless the tool is marked `alwaysAllow`
    - Response formats: markdown (default, rendered via marked) or HTML (rendered as fragment)
    - Model selector: Claude Opus 4.8, Sonnet 4.6, Haiku 4.5 (configurable in Settings)

    SECTION: Workspace Memory
    - Each workspace can have a `homeFolder` (a local directory on disk)
    - Memory stored in `{homeFolder}/memory/MEMORY.md` (index) + per-entry `.md` files with YAML frontmatter (name, description, type)
    - Claude has `read_memory(topic)` and `write_memory(name, description, type, content)` tools available in every chat session
    - A compact memory index is injected into every system prompt so Claude has ambient context
    - `write_memory` is always-allow (no per-call approval prompt)

    SECTION: Persistence
    - State debounced (200 ms) into electron-store; final synchronous flush on quit via `window.__flushAppState`
    - Schema v2 (`schemaVersion: 2`); migration in `src/state/migrate.ts` — bumps fall back to defaults, not crashes
    - Multi-workspace: array of `Workspace` objects; each has id, name, views, chatHistory, settings, homeFolder
    - Storage files: `workspaceai-state` (app state) and `workspaceai-keys` (API key, encrypted) at `~/Library/Application Support/orbit/`

    SECTION: Session Logging
    - Every app launch writes one JSON-lines file: `~/Library/Application Support/workspaceai/logs/session-<ISO>-<pid>.log`
    - At most 20 files kept (oldest rotated out)
    - Log entry shape: `{ t, level, cat, action, detail }`
    - All IPC handlers auto-logged via `withLogging()` wrapper; renderer events pushed via `window.api.logEvent`
    - To tail the latest log: `cat ~/Library/Application\ Support/workspaceai/logs/$(ls -t ~/Library/Application\ Support/workspaceai/logs/ | head -1)`
    - To filter: `grep '"cat":"chat"' <logfile>` or `grep '"level":"error"' <logfile>`

    SECTION: Dev Workflow (Run / Test / Build)
    Source scripts from package.json:
    - `npm install` then `npm run dev` — starts electron-vite in dev mode with HMR
    - `npm test` — runs Vitest unit tests
    - `npm run typecheck` — TypeScript check across both tsconfig.node.json and tsconfig.web.json
    - `npm run dist:mac` — builds and packages a macOS .dmg (arm64 + x64)

    SECTION: Adding a New View Type
    Keep the existing Case A / Case B structure but update:
    - Case A: update the `createConfig` return type note and reference `src/views/notepad/index.tsx` as a minimal example
    - Case B: fix step list — include `electron/ipc/<myview>.ts`, `electron/ipc/index.ts`, `electron/preload.ts`, `src/ipc/client.ts`, `src/views/<myview>/`, `src/App.tsx`. Remove reference to `FileTree.tsx` (that is code-view-specific, not part of the base pattern). Note that tools and `getContext` are optional fields in `registerView()`.
  </action>

  <verify>
    <automated>cd /Users/matanw/projects/workspace/workspaceai && grep -c "# Orbit" README.md</automated>
    <automated>cd /Users/matanw/projects/workspace/workspaceai && grep -c "WorkspaceAI" README.md || true</automated>
    <automated>cd /Users/matanw/projects/workspace/workspaceai && grep -c "notepad\|terminal\|browser\|pdf" README.md</automated>
    <automated>cd /Users/matanw/projects/workspace/workspaceai && grep -c "agentic\|tool_use\|streaming" README.md</automated>
    <automated>cd /Users/matanw/projects/workspace/workspaceai && grep -c "memory:readIndex\|read_memory\|homeFolder" README.md</automated>
    <automated>cd /Users/matanw/projects/workspace/workspaceai && grep -c '"cat":"chat"' README.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep "# Orbit" README.md` returns 1 result (the title)
    - `grep "WorkspaceAI" README.md` returns 0 results (fully rebranded)
    - README.md mentions all 5 view type IDs: code, browser, terminal, pdf, notepad
    - README.md contains a section describing the agentic loop and streaming (not the old stub message)
    - README.md contains a session logging section with `"cat":"chat"` as the grep example
    - README.md contains a workspace memory section mentioning homeFolder and MEMORY.md
    - README.md contains the IPC channels table with `memory:readIndex` listed
    - `grep "AI backend not configured" README.md` returns 0 results (old stub wording gone)
    - README.md contains `npm run dist:mac` in the build section
  </acceptance_criteria>

  <done>README.md is a complete, accurate reference for Orbit. Any new contributor or returning developer can understand the architecture, run/test/build the app, and extend it — all from the README alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Documentation-only plan; no code changes, no new trust boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed. |
</threat_model>

<verification>
```bash
# CLAUDE.md accuracy
grep "follow the shape" CLAUDE.md
# Must contain: t, level, cat, action, detail

# README accuracy spot checks
grep "# Orbit" README.md
grep "AI backend not configured" README.md   # must return nothing
grep -c "notepad\|terminal\|browser\|pdf" README.md  # must be > 0
grep "memory:readIndex" README.md             # IPC channels table present
grep '"cat":"chat"' README.md                # session log section correct
```
</verification>

<success_criteria>
- CLAUDE.md grep commands work against real session log files (cat/level fields are correct)
- README.md title is "# Orbit"
- README.md describes all 5 view types, the live Claude AI backend, workspace memory, session logging, and dev workflow
- No mention of "WorkspaceAI" or the old stub AI behavior in either doc
</success_criteria>

<output>
Create `.planning/phases/04-code-quality-readme/04-03-SUMMARY.md` when done.
</output>
