---
phase: 03-workspace-homepage-whatsapp
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/state/types.ts
  - src/shell/__tests__/HomepageView.test.ts
  - electron/ipc/__tests__/whatsapp.test.ts
autonomous: true
requirements: [HP-01, HP-02, HP-03, WA-01, WA-02, WA-03]

must_haves:
  truths:
    - "@whiskeysockets/baileys is installed and appears in package.json dependencies"
    - "WhatsAppAgentStatus, WhatsAppConnectionState, WhatsAppStatusEvent, WhatsAppMessageEvent types are exported from src/state/types.ts"
    - "Test scaffolds exist for HomepageView and whatsapp.ts with failing placeholders"
  artifacts:
    - path: "src/state/types.ts"
      provides: "WhatsApp type definitions alongside existing types"
      contains: "WhatsAppConnectionState"
    - path: "src/shell/__tests__/HomepageView.test.ts"
      provides: "Failing test stubs for HP-01, HP-02, HP-03"
    - path: "electron/ipc/__tests__/whatsapp.test.ts"
      provides: "Failing test stubs for WA-01, WA-02, WA-03"
  key_links: []
---

<objective>
Install @whiskeysockets/baileys, add WhatsApp type definitions to the shared types file, and create failing test scaffolds for both the HomepageView component and the whatsapp IPC service.

Purpose: Establishes the type contracts that all subsequent plans depend on, and creates test files with Red state so they can be driven Green in Plans 03-03 and 03-04.

Output: baileys in node_modules, WhatsApp types in types.ts, two new test files with failing stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-workspace-homepage-whatsapp/03-RESEARCH.md
@.planning/phases/03-workspace-homepage-whatsapp/03-PATTERNS.md
@src/state/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @whiskeysockets/baileys and extend shared types</name>
  <files>src/state/types.ts</files>
  <read_first>
    src/state/types.ts — read the full file; new types append after the existing interfaces
  </read_first>
  <action>
    1. Run: npm install @whiskeysockets/baileys

       Verify it installs cleanly with no postinstall errors and no native-addon rebuild warnings. If `node-pre-gyp` or `electron-rebuild` is triggered, stop and report.

    2. Add the following exports to src/state/types.ts (append after the last existing interface — do NOT modify any existing types):

       Export a string union type named WhatsAppConnectionState with four members: 'disconnected', 'connecting', 'qr_pending', 'connected'.

       Export an interface named WhatsAppAgentStatus with fields:
         - state: WhatsAppConnectionState
         - qrCode?: string  (base64 QR string emitted by Baileys when state === 'qr_pending')
         - lastError?: string
         - lastReplyAt?: number  (millisecond timestamp of most recent reply sent)

       Export an interface named WhatsAppStatusEvent with a single field: status: WhatsAppAgentStatus

       Export an interface named WhatsAppMessageEvent with fields: jid: string, text: string, reply: string

    Pattern source: PATTERNS.md "src/state/types.ts" section — follow the same interface style as ChatViewState.
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | head -20 || npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm install @whiskeysockets/baileys` exits 0 with no native-addon warnings
    - @whiskeysockets/baileys appears in package.json dependencies
    - src/state/types.ts exports WhatsAppConnectionState, WhatsAppAgentStatus, WhatsAppStatusEvent, WhatsAppMessageEvent
    - TypeScript typecheck passes (0 new errors)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create failing test scaffolds (Red state)</name>
  <files>
    src/shell/__tests__/HomepageView.test.ts,
    electron/ipc/__tests__/whatsapp.test.ts
  </files>
  <read_first>
    src/shell/__tests__/memory.test.ts — analog for test file structure and import patterns
    src/state/__tests__/store.test.ts — analog for vi.mock pattern and store mocking
    .planning/phases/03-workspace-homepage-whatsapp/03-RESEARCH.md (Validation Architecture section — test map for HP-01..03 and WA-01..03)
    .planning/phases/03-workspace-homepage-whatsapp/03-PATTERNS.md (HomepageView.test.ts and whatsapp.test.ts sections)
  </read_first>
  <behavior>
    HomepageView.test.ts (covers HP-01, HP-02, HP-03):
    - HP-01: "renders the workspace name" — given store with workspace name "My WS", HomepageView renders that name
    - HP-02: "lists the last 3 views" — given 5 views in store, rendered list contains exactly 3 entries (the most recent 3)
    - HP-03: "shows agent status state" — given whatsappAgentStatus.state === 'connected', rendered output contains 'connected'

    whatsapp.test.ts (covers WA-01, WA-02, WA-03):
    - WA-01: disposeWhatsApp resets agentStatus to { state: 'disconnected' } and clears conversationHistory
    - WA-02: conversation history is per-JID — turn added for jid-a does not appear in history for jid-b
    - WA-03: makeClient is exported from electron/ipc/ai.ts (import must not throw at module load time)
  </behavior>
  <action>
    Create src/shell/__tests__/HomepageView.test.ts:
    - Import describe, expect, it, vi from 'vitest'
    - The HomepageView component does not exist yet — write the three test cases as stubs using `it.todo()` or as failing assertions against a placeholder so the test file is syntactically valid and runs with failures, not import errors
    - Do NOT try to import HomepageView (it will not exist until Plan 03-04) — use placeholder describe blocks so the file parses correctly and shows Red

    Create electron/ipc/__tests__/whatsapp.test.ts:
    - Import describe, expect, it, vi from 'vitest'
    - The whatsapp module does not exist yet — all three test cases must be structured as it.todo() stubs or failing assertions against undefined imports
    - Make the file syntactically valid so vitest can collect it without crashing
    - WA-03: write as: `it('makeClient is exported from ai.ts', () => { expect(true).toBe(false) })` with a comment "will be replaced in plan 03-03 once makeClient is exported"

    Note: vitest config includes only `src/**/*.{test,spec}.ts`. The electron/ipc/__tests__/ path is outside src/. Add it to the include list in vitest.config.ts:
    include: ['src/**/*.{test,spec}.ts', 'src/**/__tests__/**/*.ts', 'electron/ipc/__tests__/**/*.ts']
  </action>
  <verify>
    <automated>npm test -- src/shell/__tests__/HomepageView.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - src/shell/__tests__/HomepageView.test.ts exists and is parseable by vitest
    - electron/ipc/__tests__/whatsapp.test.ts exists and is parseable by vitest
    - vitest.config.ts includes the electron/ipc/__tests__/ glob
    - Running `npm test -- src/shell/__tests__/HomepageView.test.ts` collects the test file without crashing (tests may be todo or failing — Red state is expected)
    - Running `npm test -- electron/ipc/__tests__/whatsapp.test.ts` collects the file without crashing
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | Third-party package install (gated by Plan 03-01 checkpoint) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | npm install @whiskeysockets/baileys | mitigate | Human-verify checkpoint in Plan 03-01 completed before this plan runs |
| T-03-02 | Information Disclosure | WhatsAppAgentStatus.qrCode in shared types | accept | qrCode is a display-only base64 string for the Electron window; never transmitted externally |
</threat_model>

<verification>
- `npm test -- src/shell/__tests__/HomepageView.test.ts` runs (Red state is acceptable)
- `npm test -- electron/ipc/__tests__/whatsapp.test.ts` runs (Red state is acceptable)
- TypeScript reports 0 new errors: `npx tsc --noEmit`
- `package.json` contains `"@whiskeysockets/baileys"` in dependencies
</verification>

<success_criteria>
baileys installed, four WhatsApp types exported from src/state/types.ts, two test scaffold files exist with Red state, vitest.config.ts covers the electron/ipc/__tests__/ path.
</success_criteria>

<output>
Create `.planning/phases/03-workspace-homepage-whatsapp/03-02-SUMMARY.md` when done.
</output>
