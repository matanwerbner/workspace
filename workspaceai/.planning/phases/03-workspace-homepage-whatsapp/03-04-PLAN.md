---
phase: 03-workspace-homepage-whatsapp
plan: 04
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - src/state/store.ts
  - src/shell/HomepageView.tsx
  - src/shell/__tests__/HomepageView.test.ts
autonomous: true
requirements: [HP-01, HP-02, HP-03]

must_haves:
  truths:
    - "AppStore has whatsappAgentStatus: WhatsAppAgentStatus (transient, not persisted) and setWhatsAppAgentStatus action"
    - "HomepageView.tsx renders workspace name from store"
    - "HomepageView.tsx renders the most recent 3 views (views.slice(-3).reverse())"
    - "HomepageView.tsx shows WhatsApp agent status card with connection state"
    - "HomepageView.tsx subscribes to onWhatsAppStatus push events on mount and unsubscribes on unmount"
    - "HP-01, HP-02, HP-03 tests pass"
  artifacts:
    - path: "src/state/store.ts"
      provides: "whatsappAgentStatus slice + setWhatsAppAgentStatus action"
      contains: "whatsappAgentStatus"
    - path: "src/shell/HomepageView.tsx"
      provides: "Homepage dashboard component"
      exports: ["HomepageView"]
    - path: "src/shell/__tests__/HomepageView.test.ts"
      provides: "Passing tests for HP-01, HP-02, HP-03"
  key_links:
    - from: "src/shell/HomepageView.tsx"
      to: "src/state/store.ts"
      via: "useAppStore selector"
      pattern: "whatsappAgentStatus"
    - from: "src/shell/HomepageView.tsx"
      to: "src/ipc/client.ts"
      via: "api.onWhatsAppStatus"
      pattern: "onWhatsAppStatus"
---

<objective>
Add the whatsappAgentStatus slice to the Zustand store and build the HomepageView component, then drive HP-01/HP-02/HP-03 tests to Green.

Purpose: The homepage is a conditional render branch (not a view type) shown when no view is active. It surfaces workspace name, recent views, and the WhatsApp agent status card with start/stop controls. This plan runs in parallel with Plan 03-03.

Output: store slice, HomepageView.tsx component, passing unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-workspace-homepage-whatsapp/03-RESEARCH.md
@.planning/phases/03-workspace-homepage-whatsapp/03-PATTERNS.md
@src/state/store.ts
@src/state/types.ts
@src/shell/MainPane.tsx
@src/shell/__tests__/HomepageView.test.ts
@src/state/__tests__/store.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add whatsappAgentStatus slice to the Zustand store</name>
  <files>src/state/store.ts</files>
  <read_first>
    src/state/store.ts — full file; understand AppStore interface (lines 40-86) and initial state block (line 123+)
    src/state/types.ts — WhatsAppAgentStatus type added in Plan 03-02
    .planning/phases/03-workspace-homepage-whatsapp/03-PATTERNS.md — "src/state/store.ts" section for the exact transient slice pattern
    src/state/__tests__/store.test.ts — BASE object at line 43; must add whatsappAgentStatus to BASE
  </read_first>
  <behavior>
    - AppStore.whatsappAgentStatus is initialized to { state: 'disconnected' }
    - AppStore.setWhatsAppAgentStatus({ state: 'connected' }) sets whatsappAgentStatus.state to 'connected'
    - whatsappAgentStatus is NOT included in the snapshot() persist call (it is transient — top-level store fields not inside the Workspace shape are not persisted by the existing snapshot logic)
    - store.test.ts BASE object includes whatsappAgentStatus: { state: 'disconnected' } so beforeEach reset continues to work
  </behavior>
  <action>
    In src/state/store.ts:

    1. Add import for WhatsAppAgentStatus from './types' (alongside existing type imports at lines 10-17).

    2. In the AppStore interface (lines 40-86), add after `lastImportMissingPaths: string[]`:
       // Transient: WhatsApp agent status (not persisted — seeded by IPC push events)
       whatsappAgentStatus: WhatsAppAgentStatus;
       setWhatsAppAgentStatus: (status: WhatsAppAgentStatus) => void;

    3. In the useAppStore create() initial state (line 123+), add after `lastImportMissingPaths: []`:
       whatsappAgentStatus: { state: 'disconnected' },
       setWhatsAppAgentStatus: (status) => set({ whatsappAgentStatus: status }),

    4. In src/state/__tests__/store.test.ts, update the BASE constant to include:
       whatsappAgentStatus: { state: 'disconnected' } as WhatsAppAgentStatus,
       Add the necessary import for WhatsAppAgentStatus from '../../state/types'.

    Do NOT touch the snapshot() function in migrate.ts — the whatsappAgentStatus field is top-level (not inside Workspace), so the existing snapshot logic already excludes it.
  </action>
  <verify>
    <automated>npm test -- src/state/__tests__/store.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - src/state/store.ts AppStore interface contains whatsappAgentStatus and setWhatsAppAgentStatus
    - Initial state has whatsappAgentStatus: { state: 'disconnected' }
    - `npm test -- src/state/__tests__/store.test.ts` passes (Green — no regressions)
    - TypeScript typecheck passes
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build HomepageView.tsx and drive HP-01/HP-02/HP-03 Green</name>
  <files>
    src/shell/HomepageView.tsx,
    src/shell/__tests__/HomepageView.test.ts
  </files>
  <read_first>
    src/shell/__tests__/HomepageView.test.ts — current scaffold state from Plan 03-02; replace stubs with real tests
    src/state/store.ts — selectActiveWorkspace, selectViews selectors
    src/state/types.ts — Workspace, WhatsAppAgentStatus types
    src/ipc/client.ts — api.onWhatsAppStatus, api.offWhatsAppStatus, api.whatsappStart, api.whatsappStop (added in Plan 03-05; in this plan use window.api directly or guard with optional chaining since the bridge methods are not yet wired — add a comment noting they will be wired in 03-05)
    .planning/phases/03-workspace-homepage-whatsapp/03-PATTERNS.md — "src/shell/HomepageView.tsx" section for component structure and useEffect pattern
    src/state/__tests__/store.test.ts — vi.mock pattern for mocking the ipc/client module
  </read_first>
  <behavior>
    HP-01: HomepageView renders the workspace name — given a store with workspace.name === 'My WS', the rendered output contains 'My WS'
    HP-02: HomepageView lists at most 3 recent views — given 5 views, rendered list contains exactly 3 items (the last 3, most-recent-first)
    HP-03: HomepageView shows the agent status state string — given whatsappAgentStatus.state === 'connected', the rendered output contains 'connected'
  </behavior>
  <action>
    Create src/shell/HomepageView.tsx:

    Imports:
    - useEffect from 'react'
    - useAppStore, selectActiveWorkspace, selectViews from '../state/store'
    - api from '../ipc/client' (for onWhatsAppStatus/offWhatsAppStatus push listeners)
    - WhatsAppAgentStatus from '../state/types'

    Export function HomepageView():

    Store reads:
    - const workspace = useAppStore(selectActiveWorkspace)
    - const views = useAppStore(selectViews)
    - const agentStatus = useAppStore((s) => s.whatsappAgentStatus)
    - const setWhatsAppAgentStatus = useAppStore((s) => s.setWhatsAppAgentStatus)

    Push event subscription (useEffect — empty dependency array or [setWhatsAppAgentStatus]):
    - Subscribe to api.onWhatsAppStatus on mount, unsubscribe via api.offWhatsAppStatus on unmount
    - Handler: (event: { status: WhatsAppAgentStatus }) => setWhatsAppAgentStatus(event.status)
    - Note: api.onWhatsAppStatus is wired in Plan 03-05; for now the call is safe as bridge() will throw if window.api is undefined — wrap in try/catch inside useEffect with a console.warn fallback

    Recent views: const recentViews = views.slice(-3).reverse()

    Render structure (className conventions follow existing shell classes):
    - Root div: className="homepage-view"
    - h1: workspace?.name ?? 'Workspace'
    - section className="recent-views":
      - h2: 'Recent Views'
      - if recentViews.length === 0: p 'No views yet.'
      - ul: recentViews.map((v) => li key={v.id} — v.name
    - section className="whatsapp-agent":
      - h2: 'WhatsApp Agent'
      - p className="agent-status": agentStatus.state  (renders the state string directly)
      - button 'Start Agent' (onClick: calls api.whatsappStart?.() — optional chain, wired in 03-05)
      - button 'Stop Agent' (onClick: calls api.whatsappStop?.() — optional chain, wired in 03-05)
      - If agentStatus.state === 'qr_pending' and agentStatus.qrCode:
          p: 'Scan QR code in WhatsApp on your phone to connect.'
          img src={`data:image/png;base64,${agentStatus.qrCode}`} alt="WhatsApp QR code"
          (Note: Baileys emits the raw QR string, not a PNG — use a qrcode library to render it in a future phase, or display as text for MVP. For MVP: render a pre element with the qrCode text value, not an img tag, to avoid CSP issues with data: images for large strings)

    Update src/shell/__tests__/HomepageView.test.ts:
    - Replace all stub/todo tests with real tests for HP-01, HP-02, HP-03
    - Mock the Zustand store (vi.mock('../state/store', ...)) to return controlled workspace + views + agentStatus
    - Mock the ipc/client module (vi.mock('../ipc/client', ...)) so api.onWhatsAppStatus and api.offWhatsAppStatus are vi.fn() no-ops
    - Run `npm install --save-dev @testing-library/react` unconditionally before writing tests (idempotent — npm skips if already present). Then use render() + screen.getByText() from @testing-library/react for assertions. Do NOT install @testing-library/jest-dom — the assertions here use only @testing-library/react, and the project is Vitest-based.
    - vitest.config.ts environment is 'node' — for React component tests, change environment to 'jsdom' OR add a test-environment comment at the top of the test file: `// @vitest-environment jsdom`
    - HP-01 test: render HomepageView with mock store returning workspace.name='My WS'; assert screen.getByText('My WS') exists
    - HP-02 test: mock selectViews to return array of 5 ViewInstance objects with distinct ids/names; assert rendered ul has exactly 3 li elements
    - HP-03 test: mock whatsappAgentStatus = { state: 'connected' }; assert screen.getByText('connected') exists in the agent-status element
  </action>
  <verify>
    <automated>npm test -- src/shell/__tests__/HomepageView.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/shell/HomepageView.tsx exists and exports HomepageView as a named export
    - npm test -- src/shell/__tests__/HomepageView.test.ts exits 0 (HP-01, HP-02, HP-03 Green)
    - HomepageView uses views.slice(-3).reverse() for the recent views list (grep confirms)
    - HomepageView subscribes to onWhatsAppStatus on mount and unsubscribes on unmount (useEffect with cleanup return)
    - TypeScript typecheck passes
    - QR code displayed as text/pre element (not img with data:image/png) to avoid CSP issues
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (invoke) | whatsappStart / whatsappStop IPC calls from homepage button clicks |
| main → renderer (push) | whatsapp:status events update homepage UI state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-HP1 | Spoofing | QR code display | mitigate | QR displayed inside Electron window only; never transmitted over network; rendered as text in MVP (not downloadable image) |
| T-03-HP2 | Information Disclosure | agentStatus.qrCode in Zustand store | accept | Transient field; not persisted; only visible within the app window |
| T-03-HP3 | Tampering | Homepage start/stop buttons | accept | Buttons invoke whatsappStart/Stop via IPC which the main process controls; no renderer-side state mutation on click |
</threat_model>

<verification>
- `npm test -- src/shell/__tests__/HomepageView.test.ts` passes (HP-01, HP-02, HP-03 Green)
- `npm test -- src/state/__tests__/store.test.ts` passes (no regressions)
- `npx tsc --noEmit` passes with 0 errors
- grep 'whatsappAgentStatus' src/state/store.ts returns matches in interface AND initial state
- grep 'slice(-3).reverse()' src/shell/HomepageView.tsx confirms view ordering
</verification>

<success_criteria>
whatsappAgentStatus store slice active, HomepageView component renders workspace name + recent 3 views + agent status, HP-01/HP-02/HP-03 tests Green, store tests still Green.
</success_criteria>

<output>
Create `.planning/phases/03-workspace-homepage-whatsapp/03-04-SUMMARY.md` when done.
</output>
