---
phase: 03-workspace-homepage-whatsapp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [HP-01, HP-02, HP-03, WA-01, WA-02, WA-03]

must_haves:
  truths:
    - "Developer has verified @whiskeysockets/baileys is legitimate before it is installed"
    - "QR pairing is understood: user must scan/enter code in WhatsApp mobile app on first run"
  artifacts: []
  key_links: []

user_setup:
  - service: whatsapp-personal
    why: "WhatsApp QR pairing — user must scan QR code in the WhatsApp app on first run to authenticate the agent"
    env_vars: []
    dashboard_config:
      - task: "Open WhatsApp on your phone → Settings → Linked Devices → Link a Device (scan QR)"
        location: "WhatsApp mobile app"
---

<objective>
Verify package legitimacy before installing @whiskeysockets/baileys.

Purpose: The Package Legitimacy Audit in RESEARCH.md flagged @whiskeysockets/baileys as [ASSUMED] — slopcheck was unavailable at research time. Per the package legitimacy gate protocol, a blocking human-verify checkpoint must run before the npm install task in Plan 03-02.

Output: Developer has confirmed the package is legitimate and the install can proceed safely.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-workspace-homepage-whatsapp/03-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify @whiskeysockets/baileys package legitimacy</name>
  <what-built>
    Nothing is installed yet. This checkpoint exists because RESEARCH.md flagged @whiskeysockets/baileys as [ASSUMED] — slopcheck was unavailable during research. Per the package legitimacy gate protocol, the developer must manually verify the package before Plan 03-02 runs the install.
  </what-built>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/@whiskeysockets/baileys and confirm:
       - The package exists under the @whiskeysockets scope
       - It has recent publishes (May/June 2026 expected)
       - Weekly download count is in the millions (it is a widely-used WhatsApp library)
       - The description and README describe a WhatsApp Web API client
    2. Visit https://github.com/WhiskeySockets/Baileys and confirm:
       - The repository is active with recent commits
       - README describes multi-device WhatsApp Web support
       - It is the same package as the npm one (linked from README)
    3. Run: npm view @whiskeysockets/baileys@6.7.x dist-tags
       Confirm a 6.7.x stable release exists and is not suspicious.
    4. Confirm the package has no native addon in the 6.x dependency list:
       Run: npm view @whiskeysockets/baileys@latest dependencies
       You should NOT see any `*-native`, `*-rust*`, or `node-gyp` entries in the 6.x deps.
  </how-to-verify>
  <resume-signal>Type "approved" after verification. If anything looks suspicious, type "abort" and do not proceed with Plan 03-02.</resume-signal>

  <read_first>
    .planning/phases/03-workspace-homepage-whatsapp/03-RESEARCH.md (Package Legitimacy Audit section)
  </read_first>
  <acceptance_criteria>
    - Developer has confirmed the npm page, GitHub repo, version, and dependency list
    - No native addons found in @whiskeysockets/baileys@6.7.x deps
    - Developer types "approved" to unblock Plan 03-02
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry | Downloading a third-party package — must be verified as legitimate before install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | npm install @whiskeysockets/baileys | mitigate | Blocking human checkpoint in this plan verifies package via npmjs.com + GitHub before install proceeds |
</threat_model>

<verification>
Checkpoint passed when developer types "approved" after completing the four manual verification steps above.
</verification>

<success_criteria>
Developer confirms @whiskeysockets/baileys is a legitimate, widely-used npm package with no native addons in 6.7.x, and types "approved" to proceed.
</success_criteria>

<output>
Create `.planning/phases/03-workspace-homepage-whatsapp/03-01-SUMMARY.md` when done.
</output>
